# SNM-008 · MEDIUM

**Migrate routes storage from TinyDB to SQLite**

| Field | Value |
|-------|-------|
| **Priority** | MEDIUM |
| **Phase** | 2 — Short Term (Within Sprint) |
| **File** | `app/routes_db.py:37` |
| **Status** | ⬜ Open |

---

## Problem

TinyDB is file-based JSON with in-process locking only. Not suitable for production write concurrency.

## Fix

```python
conn = sqlite3.connect('routes.db', isolation_level='IMMEDIATE')
conn.execute('PRAGMA journal_mode=WAL')
```

Wrap all ops in context-managed connections with commit/rollback. Add indexes on `path` and `enabled`.

## PR Checkpoints

- [ ] TinyDB dependency removed from `requirements.txt`
- [ ] `routes_db.py` rewritten using `sqlite3`
- [ ] WAL mode enabled
- [ ] Indexes created on `path` and `enabled` columns
- [ ] All existing `routes_db` unit tests pass against new implementation
- [ ] Migration note added to `CHANGELOG.md`
