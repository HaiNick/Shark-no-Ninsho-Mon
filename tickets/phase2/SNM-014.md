# SNM-014 · MEDIUM

**Fix insecure TLS verification in health checks**

| Field | Value |
|-------|-------|
| **Priority** | MEDIUM |
| **Phase** | 2 — Short Term (Within Sprint) |
| **File** | `app/caddy_manager.py:333` |
| **Status** | ⬜ Open |

---

## Problem

`verify=False` disables SSL cert validation — MITM risk.

## Fix

```python
verify_tls = not bool(route.get('insecure_skip_verify', False))
resp = requests.get(url, timeout=timeout_sec, verify=verify_tls)
```

## PR Checkpoints

- [ ] Default `verify=True` in `classify_service_status()`
- [ ] `insecure_skip_verify` per-route flag respected
- [ ] `SSLError` caught and returns `DOWN / tls_error` state
- [ ] `insecure_skip_verify` documented in `.env.template` or route schema
