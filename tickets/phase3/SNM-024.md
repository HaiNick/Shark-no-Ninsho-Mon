# SNM-024 · LOW

**Add Flask-Talisman for security headers**

| Field | Value |
|-------|-------|
| **Priority** | LOW |
| **Phase** | 3 — Long Term (Next Quarter) |
| **File** | `app/app.py` |
| **Status** | ⬜ Open |

---

## Problem

No security headers (HSTS, X-Content-Type-Options, X-Frame-Options) set by the Flask app.

## Fix

```python
from flask_talisman import Talisman
Talisman(app, content_security_policy=False)  # Enable CSP incrementally
```

## PR Checkpoints

- [ ] `Flask-Talisman` initialised in `app.py`
- [ ] Response headers include `Strict-Transport-Security`
- [ ] Response headers include `X-Content-Type-Options: nosniff`
- [ ] Response headers include `X-Frame-Options: SAMEORIGIN`
- [ ] Dashboard still loads correctly after Talisman enabled
