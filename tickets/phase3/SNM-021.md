# SNM-021 · LOW

**Fix `target_path` default value (`''` → `'/'`)**

| Field | Value |
|-------|-------|
| **Priority** | LOW |
| **Phase** | 3 — Long Term (Next Quarter) |
| **File** | `app/routes_db.py:66` |
| **Status** | ✅ Done |

---

## Problem

Default `target_path` is empty string `''` instead of `'/'`, which can cause unexpected routing behaviour.

## Fix

Change default from `''` to `'/'`. Add `target_path = target_path or '/'` guard inside the function body.

## PR Checkpoints

- [x] Default parameter changed from `''` to `'/'`
- [x] Guard `target_path = target_path or '/'` added in function body
- [x] Existing routes with empty `target_path` handled gracefully
- [x] Unit tests updated to reflect new default
