# SNM-023 · LOW

**Pin dependency versions in requirements.lock**

| Field | Value |
|-------|-------|
| **Priority** | LOW |
| **Phase** | 3 — Long Term (Next Quarter) |
| **File** | `app/requirements.txt` |
| **Status** | ✅ Done |

---

## Problem

Dependencies are not pinned to exact versions, risking unexpected breakage from upstream updates.

## Fix

Add `flask-wtf>=1.2.1` and `flask-talisman>=1.1.0`. Generate a pinned `requirements.lock` via `pip freeze`. Add `safety check` to CI workflow.

## PR Checkpoints

- [x] `flask-wtf>=1.2.1` added to `requirements.txt`
- [x] `flask-talisman>=1.1.0` added to `requirements.txt`
- [x] `requirements.lock` generated and committed
- [x] `safety check` step added to GitHub Actions workflow
